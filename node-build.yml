parameters:
- name: nodeVersion
  type: string
  default: '18.x'

steps:
- task: NodeTool@0
  displayName: 'Use Node.js'
  inputs:
    versionSpec: ${{ parameters.nodeVersion }}

- script: |
    npm ci
    npm run build
  displayName: 'Install & Build'
